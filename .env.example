# ===== 認証（ログイン必須） =====
# ランダムな文字列（32文字以上推奨）。生成例: openssl rand -base64 32
NEXTAUTH_SECRET=your-secret-at-least-32-chars
# 本番デプロイ時は Vercel の URL を設定（例: https://xxx.vercel.app）
NEXTAUTH_URL=http://localhost:3000

# ログイン用のユーザー名とパスワード（メール形式や任意の文字列で可）
AUTH_USERNAME=your-email@example.com
AUTH_PASSWORD=your-secure-password

# ===== 連絡・ファイル解析（任意） =====
# OpenAI API Key。未設定かつクローズド/ローカル未使用の場合は解析機能のみ利用不可
# https://platform.openai.com/api-keys で取得
OPENAI_API_KEY=sk-...

# クローズド運用（入力情報を外部に送らない）
# true にすると OpenAI を一切呼ばず、テキスト・PDF の抽出のみ行います。画像の AI 解析は行いません。
# CLOSED_AI_MODE=true

# 自社サーバー等の OpenAI 互換 API（Ollama / LocalAI 等）のベース URL
# 設定時はここにのみ送信し、OpenAI には送りません。OPENAI_API_KEY はローカル側で不要なら空で可。
# LOCAL_AI_BASE_URL=http://localhost:11434/v1
# ホスト型 Ollama / 互換 API で認証が必要な場合のみ
# LOCAL_AI_API_KEY=your-api-key
# ローカルAIのモデル名（Ollama: ollama list で確認。画像解析は llava など）
# LOCAL_AI_MODEL=llama3.2
